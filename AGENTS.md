# Codex working notes

- When creating or copying an HTML version, update the visible/internal version number inside the file as well as the filename or folder name, so it is always clear which version is currently being used.
